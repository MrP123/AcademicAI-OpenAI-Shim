[project]
name = "litellm-aai-provider"
version = "0.1.0"
description = "FastAPI-based shim that translates OpenAI-compatible requests into AcademicAI API requests"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.51.0",
]
